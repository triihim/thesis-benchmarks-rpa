#!/bin/bash

browsers=("chrome" "firefox" "edge")
frameworks=("react" "angular" "vue" "blazor_aot" "blazor_noaot")

cd rpa

for b in "${browsers[@]}"
do
  for f in "${frameworks[@]}"
    do
      for i in {1..10}
      do
        python ./scripts/profile.py $b $f $i
      done
    done
done